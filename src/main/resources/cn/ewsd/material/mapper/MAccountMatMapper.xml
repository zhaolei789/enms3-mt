<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.ewsd.material.mapper.MAccountMatMapper">
    <select id="getAccountMat" resultType="cn.ewsd.material.model.MAccountMat">
        SELECT * FROM m_account_mat WHERE team_no=#{teamNo} AND mat_no=#{matNo}
    </select>
</mapper>